# Reproduction notebook

The final Kaggle notebook will be generated only after the negative sample, baseline, feature
ablations, and backtest stabilize. It must:

1. verify source and processed artifact hashes;
2. show the `t_decision` feature boundary;
3. reproduce all reported tables, metrics, and figures;
4. run without private paths or credentials;
5. identify optional large post-deployment data as labels/backtest inputs only.

Notebook publication details and Kaggle resource references will be recorded here before the
final Writeup is submitted.
